#include<iostream>
using namespace std;

int main()
{
	int n;
	double sum;
	cin >> n;
	for (int i = 1; i <= n; i++)
	{
		double x, y; int people;
		cin >> x >> y >> people;
		double dist = sqrt(x * x + y * y);

	}
}